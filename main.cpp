#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;

    vector<vector<int>> cubes(n);

    for (int i = 0; i < n; i++)
    {
        vector<int> temp;
        temp.push_back(1);
        temp.push_back(2);
        temp.push_back(3);
        temp.push_back(4);
        temp.push_back(5);
        temp.push_back(6);

        cubes.push_back(temp);
    }


    for (int i = 0; i < m; i++)
    {
        int o, p;
        
    }

    return;
}